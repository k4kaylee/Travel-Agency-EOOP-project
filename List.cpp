#include "List.h"


/*
template <typename T> List<T>::List() {
	current = nullptr;
	next = nullptr;
	head = nullptr;
}

template <typename T> List<T>::~List() {
	this->free();
}

template<typename T> void List<T>::free() {
	T* curr = T->head;
	while (curr != nullptr) {
		T* temp = curr;
		curr = T->next;
		delete temp;
	}
}
*/